# MaintainFlow
A facility maintainence request web app made using React, Express, and Mongodb.

## Running
Choose one of the methods
* Use the docker compose file with `docker compose up -d` 
* Make individual images 
* Run locally with the node scripts of each project